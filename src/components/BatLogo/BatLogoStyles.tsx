import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  titulo: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#e9a629'
  },
  BatLogo: {
    resizeMode: 'contain',
    height: 180,
    width: 350
  }
});